# QuickLook 增强版维护交接

更新日期：2026-09-06。上游为 QL-Win/QuickLook，基线 tag `4.5.0`，定制分支 `enhancements`。最近部署的功能提交为 `af8219f`，包含 Everything 多选支持；文档提交不代表重新部署程序。

## 当前行为与验收

- Ins 后台复制当前文件，按扩展名分类，重名自动编号，不修改源文件；保存路径与跳转参数在托盘设置中修改。
- A/D 每次跳转 5 秒，默认长按延迟 350 毫秒、重复间隔 200 毫秒。有效预览场景消费物理按键；搜索框、重命名等输入场景不接管。
- 资源管理器和 Everything 多选 2～9 个文件，轻按空格平铺独立窗口，Esc 关闭整组。单文件方向键行为保持不变。
- 多视频同时播放，仅当前窗口有声；临时静音不写入个人音量设置。
- 用户先确认基础增强功能“我测试过了，没问题”，随后单独测试 Everything 并反馈“我试过了，没有问题”。Everything 已完成用户实际验收，不再标记为待验收。此反馈不扩写为每种输入法、多显示器或极端负载均逐项通过。
- 主程序/视频插件 Release 构建、保存文件完整性与并发重名测试、Everything 路径解析测试通过。安装及回滚四文件校验通过。既有 2052 个设置、插件和支持文件曾完成保留校验。

## 实现位置与注意事项

- `QuickLook/Enhancements` 包含设置、保存、选择读取与窗口组/键盘管理；异步读取或加载完成后须检查关闭状态与代次，避免 Esc 后重新弹窗。
- `EverythingSelection` 使用官方 WM_COMMAND 41007 获取全部选中完整路径，操作前物化剪贴板备份，读取后恢复；并发变化时不覆盖新的剪贴板内容。剪贴板历史工具仍可能记录中间路径。权限不同或响应超时必须明确报错，禁止读取旧剪贴板作为新选择。
- `SelectionPaths` 处理带引号及中文路径、去重与验证，返回 10 项用于触发整组超限提示，不静默截取 9 项。
- `IVideoPreviewControl` 是可选接口，不改变已有 IViewer 契约。视频插件读取各自文件信息，防止多窗延迟加载串用元数据。
- 资源管理器多个标签页不能唯一识别时明确提示；其他未接入的文件管理器保留原单文件功能。

## 构建、部署、回滚

应用目标仍为 **.NET Framework 4.6.2**；构建工具是 .NET 10 SDK，另需 Windows SDK 10.0.26100.0。现有独立 3MF 插件使用 net472，不应把“所有项目必须 net472”作为主程序要求。

运行 `Scripts/build-enhancements.ps1`，可通过 `-Dotnet` 指定 SDK 可执行文件。输出 `artifacts/QuickLook-4.5.0-enhancements.zip`，是 4.5.0 便携版增量补丁。上游缺少规则集文件的编译警告仍存在。

安装脚本校验清单并备份四个替换文件和完整 UserData，停止目标进程且等待退出后替换：主程序、exe.config、Common DLL、视频插件 DLL。保持安装路径、portable.lock、UserData、其他插件及根目录 assimp.dll。回滚脚本从指定备份恢复四个文件，保留当前设置和已保存文件。具体本机备份位置见安装目录 HANDOFF，不提交公开仓库。

## 后续维护

`origin` 指向用户 fork，`upstream` 指向官方。先 fetch 官方 tag，在工作分支合并目标稳定版，解决冲突并构建、回归验证后再部署。不要直接覆盖官方包。保留 3MF 插件与 assimp 修复，更新后检查上游是否已修复相关问题。

GitHub Actions 位于 `.github/workflows/enhancements.yml`；每次推送构建并测试补丁。凭据、个人路径、安装目录、UserData、日志和备份均不提交。用户仅授权此 fork 维护，未要求向官方提交 PR。
