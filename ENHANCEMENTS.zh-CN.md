# QuickLook 4.5.0 增强版

源码：https://github.com/yunxiujun/QuickLook/tree/enhancements 。基于 QL-Win/QuickLook 4.5.0，保留上游 GPL-3.0 许可。

## 使用

- **Ins**：复制当前预览文件，按扩展名进入“图片 / 视频 / 其他文件”。同名自动编号，不覆盖，不移动原文件。文件夹不递归保存。
- **A / D**：点按视频后退 / 前进 10 秒；按住 350 毫秒后以稳定的小步连续快退/快进，松手停止。长按期间底部播放时间条持续显示，松手后恢复自动隐藏。左右方向键仍用于单文件预览切换。
- **多选 + 空格**：资源管理器或 Everything 结果列表选择 2～9 个文件，轻按空格平铺独立窗口，松手保持。2 个左右、3 个三列、4 个四宫格、5～6 个三列两行、7～9 个三列三行。
- **Esc**：关闭本次整组窗口。窗口自己的关闭按钮只关闭自身。再次对同一选择按空格关闭整组；更改选择后按空格替换整组。
- 多个视频同时播放，只有当前操作窗口有声音。点击另一个窗口切换声音，图片窗口激活时视频全部静音。临时静音不保存为音量设置。
- 托盘右键 → **增强功能设置**：修改保存根目录、跳转秒数、长按延迟及重复间隔。设置保存在 `UserData/QuickLook.Enhancements.config`。

默认通用目录为系统“文档”下的 QuickLook；安装脚本的 `-DefaultSaveRoot` 可设置个人初始目录，不需要把私人路径写入源码。

## 安装与回滚

压缩包是 **4.5.0 便携版增量补丁**，不是完整官方安装包。解压后在 PowerShell 执行：

```powershell
.\install-enhancements.ps1 -TargetDirectory '你的 QuickLook 便携目录' -DefaultSaveRoot '你的保存目录'
```

安装前校验 SHA256，备份四个替换文件及完整 UserData，只替换主程序、主程序配置、Common 库和视频插件 DLL。个人设置、其他插件、3MF 插件、根目录 assimp.dll、portable.lock 与外部启动路径保持不变。

需要回滚时使用安装输出中的备份目录：

```powershell
.\rollback-enhancements.ps1 -BackupDirectory '安装时输出的备份目录'
```

回滚恢复四个二进制/配置文件，保留当前个人设置和已保存文件；备份内还保留了安装前的 UserData，供需要时单独恢复。

## 构建与跟随官方更新

Windows，需要 .NET 10 SDK 和 Windows SDK 10.0.26100.0；应用仍目标 .NET Framework 4.6.2，并未迁移到 .NET 10 运行时。

```powershell
.\Scripts\build-enhancements.ps1
```

生成 `artifacts/QuickLook-4.5.0-enhancements.zip`。GitHub Actions 同样构建补丁并运行文件保存测试。安装目录、UserData、日志、测试文件及凭据均不得提交。

维护分支为 `enhancements`，`upstream` 指向官方仓库。升级时先 `git fetch upstream --tags`，建立升级工作分支，再合并目标稳定版 tag；解决冲突后重新构建并实测，最后部署。不要直接用官方包覆盖定制主程序，否则增强功能会丢失。检查更新功能只是通知官方新版本，不会自动安装。

升级回归重点：键盘钩子和输入法、资源管理器多选、窗口异步加载取消、视频控制接口、ImageViewer 扩展名清单、所有原有插件及 3MF 修复。

## 验证记录与边界

- 主程序、Common 库与视频插件 Release 编译通过；上游工程存在“缺少 MinimumRecommendedRules.ruleset”警告。
- 文件测试覆盖中文路径、图片/视频/其他分类、并发同名副本、字节一致性、原文件保留、无效源/目标及临时文件清理。
- 用户已在本机实际操作测试，并反馈“我测试过了，没问题”。这不等同于逐项完成所有输入法、9 个视频负载和多显示器缩放组合测试。
- Everything 多选功能部署后，用户另行测试并反馈“我试过了，没有问题”，已完成用户实际验收。
- 批量功能支持 Windows 资源管理器及 Everything。资源管理器多个标签页无法唯一识别时会明确提示。Everything 使用官方 41007 命令读取多选完整路径，并备份/恢复剪贴板；剪贴板并发变化时不会覆盖新内容。剪贴板历史工具可能记录中间路径。搜索框和重命名输入不触发批量预览；其他文件管理器继续使用原单文件入口。
- 视频查看器使用 MediaInfo 判断可播放内容；保存分类采用常见视频容器扩展名清单，音频归“其他文件”，特殊/未知扩展名也归“其他文件”。
- 保存失败会报错，复制中的副本使用临时文件；系统强制断电可能留下 `.quicklook-*.partial` 文件，不会被当作成功副本。
